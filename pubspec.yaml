name: ggb_pos
description: Cloud POS system
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  charts_flutter: ^0.12.0
  connectivity_plus: ^7.1.0
  flutter_local_notifications: ^21.0.0
  flutter_local_notifications_linux: ^8.0.0
  fl_chart: ^0.66.0
  sqflite: ^2.3.0
  path: ^1.9.0   # needed for sqflite database paths

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true